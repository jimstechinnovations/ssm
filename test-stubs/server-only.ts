// Test stub for the `server-only` package. The real package throws when imported
// outside a React Server Component (e.g. under vitest/jsdom). Aliased in
// vitest.config.mts so server modules can be unit-tested directly.
export {}
